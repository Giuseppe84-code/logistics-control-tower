[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: every route is served by index.html so React Router can handle it
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
